# Demo Acceptance

- Main question returns table-backed facts for the Komsomolsky stress-strain state case.
- Gap question returns missing direct measurement or forecast validation gaps.
- Every accepted evidence claim has document id, locator, quote, and confidence.
- UI runs at `http://localhost:8501`.
- If Docker blocks, local Streamlit fallback is acceptable.

